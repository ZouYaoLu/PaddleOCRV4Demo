# PaddleOCR GPU加速版使用说明

## ✅ 项目已成功编译并可运行！

### 🎯 主要改进
1. **避免重复下载模型** - 程序会检查是否已下载过模型
2. **支持RTX 50系列** - 包含最新的sm120架构支持
3. **智能GPU检测** - 自动尝试GPU加速，失败时回退到CPU
4. **按需GPU包引用** - 根据显卡型号选择对应的包

### 🚀 快速开始

#### 1. 当前状态（CPU模式）
项目现在可以正常编译和运行，默认使用CPU模式。

#### 2. 启用GPU加速（可选）
如果你想启用GPU加速，根据显卡型号编辑 `src/PaddleOCRApp.csproj`：

**RTX 30系列（如RTX 3060/3070/3080/3090）**
```xml
<!-- 取消注释这行 -->
<PackageReference Include="Sdcb.PaddleInference.runtime.win64.cu129_cudnn910_sm86" Version="3.1.0.54" />
```

**RTX 40系列（如RTX 4060/4070/4080/4090）**
```xml
<!-- 取消注释这行 -->
<PackageReference Include="Sdcb.PaddleInference.runtime.win64.cu129_cudnn910_sm89" Version="3.1.0.54" />
```

**RTX 50系列（如RTX 5060/5070/5080/5090）**
```xml
<!-- 取消注释这行 -->
<PackageReference Include="Sdcb.PaddleInference.runtime.win64.cu129_cudnn910_sm120" Version="3.1.0.54" />
```

**RTX 20系列/GTX 16xx系列**
```xml
<!-- 取消注释这行 -->
<PackageReference Include="Sdcb.PaddleInference.runtime.win64.cu129_cudnn910_sm75" Version="3.1.0.54" />
```

**GTX 10系列**
```xml
<!-- 取消注释这行 -->
<PackageReference Include="Sdcb.PaddleInference.runtime.win64.cu129_cudnn910_sm61" Version="3.1.0.54" />
```

#### 3. 安装CUDA环境（仅GPU模式需要）
- 下载安装 [CUDA 12.9](https://developer.nvidia.com/cuda-downloads)
- 下载安装 [cuDNN 9.1.0](https://developer.nvidia.com/cudnn)
- 确保NVIDIA驱动是最新版本

#### 4. 重新编译（如果修改了GPU包）
```bash
dotnet clean src/PaddleOCRApp.csproj
dotnet build src/PaddleOCRApp.csproj
```

### 📁 模型存储说明

**重要说明**：PaddleSharp的模型管理机制如下：
- **实际模型文件**：保存在系统缓存目录（如 `C:\Users\用户名\.cache\paddle\`）
- **标记文件**：程序会在 `models` 文件夹创建标记文件来跟踪下载状态
- **共享机制**：多个PaddleSharp应用可以共享同一套模型文件

这样的设计有以下优点：
- 节省磁盘空间（多个应用共享模型）
- 自动管理模型版本
- 避免重复下载

### 🎮 使用步骤

1. **启动应用程序**
   ```bash
   dotnet run --project src/PaddleOCRApp.csproj
   ```

2. **首次使用**
   - 点击"下载模型"按钮
   - 等待模型下载完成
   - 程序会显示设备信息（CPU或GPU）

3. **加载图片**
   - 点击"选择图片"按钮
   - 选择要识别的图片文件

4. **开始识别**
   - 点击"开始OCR"按钮
   - 查看识别结果

### ⚡ 性能对比

- **CPU模式**：适合偶尔使用，无需额外配置
- **GPU模式**：识别速度提升3-10倍，适合大量OCR任务

### 🔧 故障排除

#### 编译错误
- 确保只启用了一个GPU包
- 检查包版本是否正确

#### GPU不工作
- 检查CUDA和cuDNN安装
- 确认启用了正确的GPU包
- 查看程序显示的错误信息

#### 模型下载失败
- 检查网络连接
- 重试下载
- 查看错误详情

### 📊 支持的显卡架构

| 显卡系列 | 计算能力 | 包名后缀 | 示例显卡 |
|---------|---------|---------|---------|
| GTX 10系列 | 6.1 | sm61 | GTX 1060, 1070, 1080 |
| RTX 20/GTX 16xx | 7.5 | sm75 | RTX 2070, GTX 1660 |
| RTX 30系列 | 8.6 | sm86 | RTX 3070, 3080, 3090 |
| RTX 40系列 | 8.9 | sm89 | RTX 4070, 4080, 4090 |
| RTX 50系列 | 12.0 | sm120 | RTX 5070, 5080, 5090 |

### 🎉 总结

项目已经成功迁移到PaddleSharp并支持最新的GPU架构。你现在可以：

1. **立即使用CPU模式** - 无需任何额外配置
2. **按需启用GPU加速** - 根据显卡型号选择对应包
3. **享受智能模型管理** - 避免重复下载，自动检测已有模型

开始使用吧！🚀
